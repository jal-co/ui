---
title: Status Indicator
description: Operational status badge with colored dot and label for dashboards, status pages, and header bars.
---

export const sourceFiles = ["registry/status-indicator/status-indicator.tsx"]

<ComponentDocsPage
  title="Status Indicator"
  description="Operational status badge with colored dot and label for dashboards, status pages, and header bars."
  registryName="status-indicator"
  sourceFiles={sourceFiles}
  preview={
    <div className="flex flex-wrap gap-2">
      <StatusIndicator status="operational" />
      <StatusIndicator status="degraded" />
      <StatusIndicator status="major-outage" />
      <StatusIndicator status="maintenance" />
    </div>
  }
  usage={
    <>
      <CodeLine code={`import { StatusIndicator } from "@/components/status-indicator"`} />
      <CodeLine code={`<StatusIndicator status="operational" />`} />
      <p className="text-sm text-muted-foreground">
        Server component — no client JS required. The label is
        auto-generated from the status value. Pass a custom{" "}
        <code className="rounded bg-muted px-1 py-0.5 text-xs">label</code>{" "}
        to override.
      </p>
    </>
  }
>

<section className="flex flex-col gap-4">
  <h2 className="text-xl font-semibold tracking-tight">Examples</h2>

  <div className="flex flex-col gap-4">
    <h3 className="text-base font-medium">All statuses</h3>
    <p className="text-sm text-muted-foreground">
      Seven built-in statuses with semantic coloring.
    </p>
    <VariantGrid
      registryName="status-indicator"
      files={sourceFiles}
      columns={1}
      fullWidth
      items={[
        {
          label: "All statuses",
          code: `<div className="flex flex-wrap gap-2">
  {["operational", "degraded", "partial-outage", "major-outage", "maintenance", "incident", "unknown"].map((s) => (
    <StatusIndicator key={s} status={s} />
  ))}
</div>`,
          preview: (
            <div className="flex flex-wrap gap-2">
              <StatusIndicator status="operational" />
              <StatusIndicator status="degraded" />
              <StatusIndicator status="partial-outage" />
              <StatusIndicator status="major-outage" />
              <StatusIndicator status="maintenance" />
              <StatusIndicator status="incident" />
              <StatusIndicator status="unknown" />
            </div>
          ),
        },
      ]}
    />
  </div>

  <div className="flex flex-col gap-4">
    <h3 className="text-base font-medium">Sizes</h3>
    <VariantGrid
      registryName="status-indicator"
      files={sourceFiles}
      columns={1}
      fullWidth
      items={[
        {
          label: "Small / Medium / Large",
          code: `<StatusIndicator status="operational" size="sm" />
<StatusIndicator status="operational" size="md" />
<StatusIndicator status="operational" size="lg" />`,
          preview: (
            <div className="flex flex-wrap items-center gap-2">
              <StatusIndicator status="operational" size="sm" />
              <StatusIndicator status="operational" size="md" />
              <StatusIndicator status="operational" size="lg" />
            </div>
          ),
        },
      ]}
    />
  </div>

  <div className="flex flex-col gap-4">
    <h3 className="text-base font-medium">Custom labels</h3>
    <VariantGrid
      registryName="status-indicator"
      files={sourceFiles}
      columns={1}
      fullWidth
      items={[
        {
          label: "Service-specific labels",
          code: `<StatusIndicator status="operational" label="API" />
<StatusIndicator status="degraded" label="Database" />
<StatusIndicator status="maintenance" label="CDN" />`,
          preview: (
            <div className="flex flex-wrap gap-2">
              <StatusIndicator status="operational" label="API" />
              <StatusIndicator status="degraded" label="Database" />
              <StatusIndicator status="maintenance" label="CDN" />
            </div>
          ),
        },
      ]}
    />
  </div>

  <div className="flex flex-col gap-4">
    <h3 className="text-base font-medium">Status page row</h3>
    <p className="text-sm text-muted-foreground">
      Compose with other elements for a status page layout.
    </p>
    <VariantGrid
      registryName="status-indicator"
      files={sourceFiles}
      columns={1}
      fullWidth
      items={[
        {
          label: "Composed layout",
          code: `<div className="flex flex-col divide-y rounded-xl border">
  {services.map(({ name, status }) => (
    <div key={name} className="flex items-center justify-between px-4 py-3">
      <span className="text-sm font-medium">{name}</span>
      <StatusIndicator status={status} size="sm" />
    </div>
  ))}
</div>`,
          preview: (
            <div className="flex flex-col divide-y divide-border/40 rounded-xl border border-border/60 bg-card shadow-sm">
              <div className="flex items-center justify-between px-4 py-3">
                <span className="text-sm font-medium text-foreground">API</span>
                <StatusIndicator status="operational" size="sm" />
              </div>
              <div className="flex items-center justify-between px-4 py-3">
                <span className="text-sm font-medium text-foreground">Database</span>
                <StatusIndicator status="operational" size="sm" />
              </div>
              <div className="flex items-center justify-between px-4 py-3">
                <span className="text-sm font-medium text-foreground">CDN</span>
                <StatusIndicator status="degraded" size="sm" />
              </div>
              <div className="flex items-center justify-between px-4 py-3">
                <span className="text-sm font-medium text-foreground">Search</span>
                <StatusIndicator status="maintenance" size="sm" />
              </div>
            </div>
          ),
        },
      ]}
    />
  </div>
</section>

<section className="flex flex-col gap-4">
  <h2 className="text-xl font-semibold tracking-tight">API Reference</h2>
  <ApiRefTable
    title="StatusIndicator"
    props={[
      {
        name: "status",
        type: '"operational" | "degraded" | "partial-outage" | "major-outage" | "maintenance" | "incident" | "unknown"',
        required: true,
        description: "Current operational status.",
      },
      {
        name: "label",
        type: "string",
        description: 'Custom label text. Auto-generated from status when omitted (e.g. "operational" → "Operational").',
      },
      {
        name: "size",
        type: '"sm" | "md" | "lg"',
        description: 'Badge size. Defaults to "md".',
      },
      {
        name: "className",
        type: "string",
        description: "Additional CSS classes on the root element.",
      },
    ]}
  />
</section>

<section className="flex flex-col gap-4">
  <h2 className="text-xl font-semibold tracking-tight">Notes</h2>
  <ul className="list-disc space-y-2 pl-6 text-sm text-muted-foreground">
    <li>
      <strong className="text-foreground">Server component.</strong> No{" "}
      <code className="rounded bg-muted px-1 py-0.5 text-xs">&quot;use client&quot;</code>{" "}
      — renders entirely on the server with zero client JS.
    </li>
    <li>
      <strong className="text-foreground">No dependencies.</strong> Uses
      only React, Tailwind CSS, and CVA.
    </li>
    <li>
      <strong className="text-foreground">
        <code className="rounded bg-muted px-1 py-0.5 text-xs">role=&quot;status&quot;</code>
      </strong>{" "}
      is applied to the root element for screen reader announcements.
    </li>
    <li>
      <strong className="text-foreground">Composable.</strong> Use
      alongside other elements for status page rows, dashboard headers,
      or navbar indicators.
    </li>
  </ul>
</section>

</ComponentDocsPage>
